{
  "name": "vimm-save-exporter",
  "type": "module",
  "private": "true",
  "scripts": {
    "test": "jest",
    "build:dev": "rm -rf dist && webpack --mode=development && cp src/adapters/primary/extension/* dist",
    "build:watch": "rm -rf dist && mkdir dist && cp src/adapters/primary/extension/* dist && webpack --mode=development --watch",
    "build:prod": "rm -rf dist && webpack --mode=production && cp src/adapters/primary/extension/* dist"
  },
  "author": "foobarfarm",
  "license": "UNLICENCED",
  "devDependencies": {
    "@babel/core": "^7.21.0",
    "@babel/preset-env": "^7.20.2",
    "@babel/preset-typescript": "^7.21.0",
    "@types/jest": "^29.4.0",
    "babel-jest": "^29.4.3",
    "inversify": "^6.0.1",
    "jest": "^29.4.3",
    "jest-environment-jsdom": "^29.4.3",
    "prettier": "^2.8.4",
    "reflect-metadata": "^0.1.13",
    "ts-jest": "^29.0.5",
    "ts-loader": "^9.4.2",
    "typescript": "^4.9.5",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  }
}
